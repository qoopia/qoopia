{
  "name": "lcm-mcp-server",
  "version": "2.1.0",
  "description": "LCM — persistent conversation memory for Claude Code agents. SQLite + full-text search.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
